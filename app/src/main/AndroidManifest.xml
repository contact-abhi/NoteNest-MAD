<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notenest">

    <!-- Required permissions for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permissions for file access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@drawable/ic_home"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteNest">

        <!-- MainActivity - Splash/Redirect Activity - Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Signup Activity -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" />

        <!-- Home Activity with bottom navigation -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Add Note Activity -->
        <activity
            android:name=".AddNoteActivity"
            android:exported="false" />

        <!-- Note Detail Activity -->
        <activity
            android:name=".NoteDetailActivity"
            android:exported="false" />

        <!-- Create Section Activity -->
        <activity
            android:name=".CreateSectionActivity"
            android:exported="false" />
    </application>

</manifest>
